// src/lib/api.ts
// COMPATIBILITY SHIM — keeps old imports working
// All imports like: import { api } from '@/lib/api'
// now get the same unified client as '@/lib/api/client'
export { api } from '@/lib/api/client';
