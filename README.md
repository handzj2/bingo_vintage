# Bingo Vintage

Motorcycle lending platform.

## Structure

```
bingo_vintage/
├── backend/    — NestJS API (deploy to Railway)
├── frontend/   — Next.js app (deploy to Vercel)
└── railway.toml
```

## Backend (Railway)
- Root Directory: `backend`
- Build: `npm run build`
- Start: `node dist/main`

## Frontend (Vercel)
- Root Directory: `frontend`
- Build: `npm run build`
- Install: `npm install`
